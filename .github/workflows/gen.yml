name: Gen

on:
  push:
    branches:
      - main

jobs:
  gen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./actions
        with:
          uma: 50,10,-10,-30
          record: ./day
